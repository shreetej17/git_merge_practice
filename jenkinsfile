pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code pulled from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo 'Build step running'
            }
        }

        stage('Test') {
            steps {
                echo 'Test cases running'
            }
        }
    }
}
