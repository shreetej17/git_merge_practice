pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build started'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests'
              
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying appp'
            }
        }
    }
}
